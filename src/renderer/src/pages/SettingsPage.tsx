import { useEffect,useState } from 'react'; export default function SettingsPage(){const [s,setS]=useState<any>({}); useEffect(()=>{(window as any).api.settings.get().then(setS)},[]); return <div className='space-y-2'><p>Caminho perfil: {s.profilePath}</p><p>Estado captura: {s.captureStatus}</p><button className='border px-3 py-2' onClick={()=> (window as any).api.settings.openDataFolder()}>Abrir pasta de dados local</button><button className='border px-3 py-2' onClick={()=> (window as any).api.session.clearComments()}>Limpar comentários da sessão atual</button></div>}
